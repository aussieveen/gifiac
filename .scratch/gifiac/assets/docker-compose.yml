services:
  gifiac:
    # Replace YOUR_GITHUB_USERNAME with the GitHub user/org the repo lives under.
    # Built and published by .github/workflows/docker-publish.yml on every push to main.
    # Pin to a specific build instead of :latest by swapping in a yyyy.mm.dd.hh.mm tag.
    image: ghcr.io/YOUR_GITHUB_USERNAME/gifiac:latest
    container_name: gifiac
    restart: unless-stopped
    ports:
      - "8123:8080"
    volumes:
      - /mnt/user/appdata/gifiac:/data
    environment:
      GIFIAC_VIDEO_DIR: /data/videos
      GIFIAC_DB_PATH: /data/gifiac.db
      R2_ACCOUNT_ID: "${R2_ACCOUNT_ID}"
      R2_ACCESS_KEY_ID: "${R2_ACCESS_KEY_ID}"
      R2_SECRET_ACCESS_KEY: "${R2_SECRET_ACCESS_KEY}"
      R2_BUCKET_NAME: "${R2_BUCKET_NAME}"
      R2_PUBLIC_BASE_URL: "${R2_PUBLIC_BASE_URL}"
